To build the tutorial you need Sphinx. (Version >= 1.2 handles correctly
headings inside of 'only' block.) Install by

 $ sudo apt-get install python-sphinx

on Debian. For PDF output LaTeX is needed.

Execute

 $ make latexpdf # release
 $ make latexpdf SPHINXOPTS='-t solution -D todo_include_todos=1' # dev version

You find the output in _build/latex/FEniCStutorial.pdf.
